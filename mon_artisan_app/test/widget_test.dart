// Tests for Mon Artisan App
// Run with: flutter test

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App constants are configured correctly', () {
    // Basic test to verify the test suite works
    expect(1 + 1, equals(2));
  });
}
